# Requirements cho Camera Agent với cải tiến

opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
timm>=0.9.0  # For efficient backbones in unified model

# Face recognition 
insightface>=0.7.3 
onnxruntime-gpu>=1.16.0 

# Hand detection
mediapipe>=0.10.0

# Tracking 
# yolox>=0.3.0  # ByteTrack tracker (cài riêng từ source)
lap>=0.4.0  # Linear assignment cho tracking
cython-bbox>=0.1.3  # Fast bbox operations

# Utils
filterpy>=1.4.5  # Kalman filter backup
pandas>=2.0.0
Pillow>=10.0.0

# API
openai>=1.0.0
pydantic>=2.0.0

# Optional - Edge optimization
onnx>=1.14.0
# tensorrt>=8.6.0  # Cài riêng từ NVIDIA index nếu cần

# Additional dependencies from imports
shapely>=2.0.0
facenet-pytorch>=2.5.3
